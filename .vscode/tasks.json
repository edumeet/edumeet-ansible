{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "galaxy",
            "type": "shell",
            "command": "ansible-galaxy install -r requirements.yml"
        },
        {
            "label": "main",
            "type": "shell",
            "command": "ansible-playbook -b -i hosts -u debian main.yml",
            "problemMatcher": []
        }
    ]
}